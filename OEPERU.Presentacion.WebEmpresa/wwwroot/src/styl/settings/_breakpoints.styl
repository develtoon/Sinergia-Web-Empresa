$mq-base-font-size = 16px

$mq-breakpoints = {
    tiny: 360px,
    small: 768px,
    medium: 996px,
    large: 1280px
}

/* className {
  background-color: white;

  +mq($until: 'small') {
    background-color: red;
  }
} */
